%ul.search_results
  - cache_fragment("search/#{params[:query]}") do
    - @client.search(params[:query]).results.each do |search_result|
      %li
        %a{:href => search_result.design_detail_page_url}
          %img{:src => search_result.design_url}
